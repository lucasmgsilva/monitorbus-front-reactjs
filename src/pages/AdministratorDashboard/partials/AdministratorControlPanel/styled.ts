import styled from 'styled-components';

export const AdministratorSection = styled.section`
    background-color: var(--secondary);
    padding: 25px;
    color: var(--secondary);
`
export const AdministratorPanel = styled.div`
    background-color: var(--white);
    border-radius: 25px;
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
`

export const AdministratorPanelBody = styled.div`
    font-size: 1rem;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    flex: 1;
`

export const Title = styled.h1`
    font-size: 2.3rem;
    font-family: 'Indie Flower', cursive;
    text-align: center;
    padding-top: 20px;
`